# macOS metadata
.DS_Store
**/.DS_Store

# Local master backup — kept outside the deploy, never upload
iCOMIC-MOTION-Website-backups/
../iCOMIC-MOTION-Website-backups/
icomic-hero.mp4

# Full-length source audio — only the trimmed system-online-loop.mp3 deploys
system-online.mp3

# Previous hero video — kept locally as a backup until the new one is confirmed live
icomic-hero-web-OLD.mp4
